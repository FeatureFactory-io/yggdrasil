name: Promote to Production

on:
  workflow_dispatch:

jobs:
  promote:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: EB CNAME swap
        run: echo "Promote — configure make swap / swap-environment-cnames"
